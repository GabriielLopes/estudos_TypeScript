// gera um erro quando o tipo any está implicito, quando o Ts não sabe qual tipo colocar
// o tipo any, é um tipo que não devemos utilizar, Somente em último caso.
/*function showMessage(msg) {
  return msg;
}

console.log(showMessage([1, 2, 3]))
*/
